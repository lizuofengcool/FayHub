# 开发环境配置文件
database:
  type: postgresql
  host: "${FAYHUB_DB_HOST:-localhost}"
  port: ${FAYHUB_DB_PORT:-5432}
  username: "${FAYHUB_DB_USERNAME:-fayhub}"
  password: "${FAYHUB_DB_PASSWORD}"
  database: "${FAYHUB_DB_NAME:-fayhub}"
  charset: utf8mb4
  max_idle_conns: 10
  max_open_conns: 100
  conn_max_lifetime_sec: 3600
  conn_max_idle_time_sec: 600

server:
  port: ${FAYHUB_SERVER_PORT:-8080}
  mode: debug
  cors_origins:
    - "http://localhost:3000"
    - "http://localhost:8080"
    - "http://127.0.0.1:3000"
    - "http://127.0.0.1:8080"
  cors_allow_all: false
  cors_allow_credentials: true

jwt:
  secret: "${FAYHUB_JWT_SECRET}"
  expire: 168
  issuer: fayhub
  algorithm: RS256
  private_key_path: config/jwt_rs256_private.pem
  public_key_path: config/jwt_rs256_public.pem

logging:
  level: info
  format: json
  output: stdout
  file:
    path: ./logs
    max_size: 100
    max_backups: 10
    max_age: 30

multi_tenant:
  mode: shared

security:
  max_login_attempts: 5
  lock_duration_min: 15

system:
  service_token: "${FAYHUB_SERVICE_TOKEN}"

plugin_engine:
  http_timeout_sec: 30

plugin_sign:
  public_key_path: ""

redis:
  enabled: true
  host: localhost
  port: 6379
  password: ""
  db: 0
  pool_size: 10

sso:
  clients:
    market: "${FAYHUB_SSO_MARKET_SECRET}"

domains:
  admin_url: "https://admin.fayhub.com"
  market_url: "https://www.fayhub.com"
  market_api_url: "http://localhost:3001"
  dev_url: "https://dev.fayhub.com"
  api_url: "https://api.fayhub.com"
  sso_url: "https://sso.fayhub.com"
  www_url: "https://www.fayhub.com"